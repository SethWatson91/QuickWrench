{
  "name": "quickwrench",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install-all": "npm --prefix client install && npm --prefix server install",
    "start": "echo 'Use two terminals: npm run dev in client/ and server/'"
  },
"devDependencies": {
  "concurrently": "^8.2.0"
}
}
